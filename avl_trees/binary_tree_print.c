#include "binary_trees.h"
#include <stdio.h>

/* Stub pour éviter une dépendance au vrai printer fourni par l'école.
 * Ne fait qu’afficher un en-tête simple afin de ne pas polluer la sortie.
 * Tu peux remplacer ce fichier par le vrai `binary_tree_print.c` si tu l’as.
 */
void binary_tree_print(const binary_tree_t *tree)
{
	(void)tree;
	/* Pas d’affichage détaillé : stub volontairement vide */
}

